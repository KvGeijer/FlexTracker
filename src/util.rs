pub struct Time {
    pub hours: usize,
    pub minutes: usize
}

pub struct Period {
    pub from: Time,
    pub to: Time
}

pub struct Date {
    pub day: usize,
    pub month: usize,
    pub year: usize
}